import { Cart } from "./cart"
describe('Basic - Cart service that can manage items.', () => {
  let cart: Cart
  beforeEach(() => {
    cart = Cart.create("CUSTOMER_01")
  })
  it('test', () => {
  });

});
